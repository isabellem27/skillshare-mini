
<header>
    <nav>
        <a class="btnMenu" id="lstUser">Liste des utilisateurs</a>
        <a class="btnMenu" id="lstSkill">Liste des compétences</a>

        {% if is_granted('IS_AUTHENTICATED_FULLY') %} 
            <a href="" class="btnMenu" id="monProfil">Mon profil</a>       
            <a href="" class="btnMenu" id="mesSkill">Modifier les compétences</a>          
            <span class="identity">{{app.user.firstName}} {{app.user.lastName}}</span>
            <a class="btnMenu"  href="{{ path('app_logout')}}">Se déconnecter</a>
        {% else %}    
            <a class="btnMenu" href="{{ path('app_login')}}">Se connecter</a>
            <a class="btnMenu" href="{{ path('app_register')}}">S'enregistrer</a>
        {% endif %}

    </nav>
</header>
